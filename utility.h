#include <iostream>
#include "game.h"
#include <string>

// "Include guard" prevents multiple inclusion:
#ifndef UTILITY_H_ALREADY_INCLUDED
#define UTILITY_H_ALREADY_INCLUDED

int utility(std::vector<std::vector<char>> layout);

// End include guard:
#endif